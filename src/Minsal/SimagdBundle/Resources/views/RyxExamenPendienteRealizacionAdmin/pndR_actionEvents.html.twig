{% autoescape false %}

	<script type="text/javascript">

        /////////////////////////////////////////////////////
        //////// EXAMS WORKLIST - CONTEXT MENU
        /////////////////////////////////////////////////////
        (function ($) {
            /*
             *
             * @type Array
             * Global variables for plugin
             */
            window.__UNREALIZEDPROCEDURESWORKLIST_MENU_ACTIONEVENTS__ = {
                'click .unrealizedproceduresworklist_show_action': function (e, value, row, index) {
                    window.console.log(e, value, row, index);
                    var url = Routing.generate('simagd_sin_realizar_show', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .unrealizedproceduresworklist_edit_action': function (e, value, row, index) {
                    window.console.log(e, value, row, index);
                    var url = Routing.generate('simagd_sin_realizar_edit', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .unrealizedproceduresworklist_delete_action': function (e, value, row, index) {
                    window.console.log(e, value, row, index);
                    var url = Routing.generate('simagd_sin_realizar_delete', {id: row.id, confirmation: false});
                    // window.location.href = url; // simulate link click
                },
                'click .unrealizedproceduresworklist_save_action': function (e, value, row, index) {
                    window.console.log(e, value, row, index);
                    var url = Routing.generate('simagd_sin_realizar_save', {id: row.id, confirmation: false});
                    // window.location.href = url; // simulate link click
                },
                'click .unrealizedproceduresworklist_saveandsearchinpacs_action': function (e, value, row, index) {
                    window.console.log(e, value, row, index);
                    var url = Routing.generate('simagd_sin_realizar_saveandsearchinpacs', {id: row.id, confirmation: false});
                    // window.location.href = url; // simulate link click
                },
                'click .unrealizedproceduresworklist_goto_action': function (e, value, row, index) {
                    window.console.log(e, value, row, index);
                    var url = Routing.generate('simagd_realizado_create', {id: row.id, worklist_id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .unrealizedproceduresworklist_studydownload_action': function (e, value, row, index) {
                    window.console.log(e, value, row, index);
                    var url = Routing.generate('simagd_sin_realizar_create', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .unrealizedproceduresworklist_create_action': function (e, value, row, index) {
                    window.console.log(e, value, row, index);
                    var url = Routing.generate('simagd_sin_realizar_create', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
            };   // --| build data collection

        }(jQuery));
        
    </script>

{% endautoescape %}