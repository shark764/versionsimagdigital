{# empty Twig template #}

{#    <script type="text/javascript">
        jQuery(document).ready(function() {
            
            var idAreaExamenEstabPadre = '-1';
            var idProyeccionPadre = '-1';
            
            {% if object.idAreaExamenEstab is not null %}
                
                idAreaExamenEstabPadre = {{ object.idAreaExamenEstab.getId()|escape('js') }};
                
                console.log(idAreaExamenEstabPadre );
                
            {% endif %}
            
            {% if object.idProyeccion is not null %}
                
                idProyeccionPadre = {{ object.idProyeccion.getId()|escape('js') }};
                
                console.log(idProyeccionPadre );
                
            {% endif %}

	    /** envío de datos para Action que muestra info-modal */
	    $("div[id='custom-entity-content-body']")
                        .load( Routing.generate('simagd_imagenologia_digital_mostrarInformacionModal',
                                    {   selectorAliasModal: '{{ _entityCustomAlias }}',
                                        idAreaExamenEstabPadre: idAreaExamenEstabPadre,
                                        idProyeccionPadre: idProyeccionPadre,
                                    }
                            ));
            
        });
        
    </script>#}