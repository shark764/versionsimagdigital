{# empty Twig template #}
    
    {# Recuperación de estudio #}
    {% if ( estudioPacienteObject is not null ) %}
        {% include 'MinsalSimagdBundle:RyxEstudioPorImagenesAdmin:est_recuperarEstudio_button.html.twig'
                                ignore missing with { 'estudioPaciente': estudioPacienteObject } %}
    {% endif %}