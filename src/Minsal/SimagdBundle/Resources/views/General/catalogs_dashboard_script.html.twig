{% autoescape false %}

    <script type="text/javascript">
        var _fn_show_object_detail = function(el, slug, id) {
            window.alert(slug + '\n' + id);
        }

        var generate = function(el, slug, emrg/* = 0, type = 'list'*/) {
			if (emrg === undefined) {
		        emrg = 0;
		    }
            ////////
            var $table = jQuery('#ryx_target_bstable');
            ////////
            $table.bootstrapTable('showLoading');

            $url = null;
			switch (slug) {
				case 'radiological_examinations':
					$url = 'simagd_proyeccion_generateTable';
					break;
				case 'local_radiological_examinations':
					$url = 'simagd_proyeccion_establecimiento_generateTable';
					break;
				case 'materials':
					$url = 'simagd_material_generateTable';
					break;
				case 'local_materials':
					$url = 'simagd_material_local_generateTable';
					break;
				case 'pacs_server_connections':
					$url = 'simagd_pacs_generateTable';
					break;
				case 'radiological_diagnostic_patterns':
					$url = 'simagd_patron_diagnostico_generateTable';
					break;
				default:
					break;
			}

            // if (slug === 'local_radiological_examinations') {
            //     emrg
            // }

            var type = jQuery('input[name=_catalogs_dashboard_display_type]:checked').val();

            $.ajax({
                type: 'POST',
                dataType: 'json',
                url: Routing.generate($url),
                data: {
                    _xml_http_request: true,
                    id: 'id',
                    slug: slug,
                    type: type,
                    emrg: emrg,
                },
                success: function(response) {
                    ////////
                    $.each(response.options, function(i, opt) {
                        if (typeof opt === 'string' && opt.indexOf('__FUNCTIONS_CALL__.functions') !== -1) {
                            var namespaces = opt.split('.');
                            var func = namespaces.pop();
                            var context = window;
                            for(var x = 0; x < namespaces.length; x++) {
                                context = context[namespaces[x]];
                            }
                            response.options[i] = context[func];  // set function
                        }
                    });
                    ////////

                    $table
                        .bootstrapTable('destroy')
                        .bootstrapTable(response.options);
                    // $table.bootstrapTable('refreshOptions', response.options);

                    // $('#ryx_target_bstable').scrollTop(25000);
                },
                error: function(err) {
                    console.log(err.error, err.responseText);
                }
            });
        }

        jQuery(document).ready(function() {
            /*
             *
             * @type type
             * Get Table definition
             */
             // generate(null, 'radiological_examinations');
             // jQuery('#')

            //////
            var $table = jQuery('#ryx_target_bstable');
            ////////
            // $table.bootstrapTable('showLoading');

            ////////
            var DEFAULT_TABLE_OPTIONS = JSON.parse('{{ DEFAULT_TABLE_OPTIONS|json_encode|e('js')|raw }}');
            $.each(DEFAULT_TABLE_OPTIONS, function(i, opt) {
                if (typeof opt === 'string' && opt.indexOf('__FUNCTIONS_CALL__.functions') !== -1) {
                    var namespaces = opt.split('.');
                    var func = namespaces.pop();
                    var context = window;
                    for(var x = 0; x < namespaces.length; x++) {
                        context = context[namespaces[x]];
                    }
                    DEFAULT_TABLE_OPTIONS[i] = context[func];  // set function
                }
            });
            ////////

            $table
                .bootstrapTable('destroy')
                .bootstrapTable(DEFAULT_TABLE_OPTIONS);


            //////////////////////////////////////////////////////////////////////////
            //////// change content type - worklist
            //////////////////////////////////////////////////////////////////////////
            var __DOM__worklistDisplayType = jQuery('input[name=_catalogs_dashboard_display_type]');

            __DOM__worklistDisplayType.on('ifChecked', function(e) {

                        jQuery('#_catalogs_dashboard_display_items li.active').find('a').triggerHandler('click');

            });
            //////////////////////////////////////////////////////////////////////////
            //////////////////////////////////////////////////////////////////////////


            // jQuery('#_switch_display').iCheck('destroy').bootstrapSwitch();

        });

        (function ($) {
            /*
             *
             * @type Array
             * Global variables for plugin
             */
            window.__FUNCTIONS_CALL__ = {};   // --| build data collection

            /*
             * Collection of functions / fc options
             */
            __FUNCTIONS_CALL__.functions = {
                onContextMenuItem: function(row, $el) {
                    /////////////////////////////////////////////////////
                    //////// RADIOLOGICAL EXAMINATIONS - CONTEXT MENU
                    /////////////////////////////////////////////////////
                    if($el.data('item') === 'radiologicalexaminations_show')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_show', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'radiologicalexaminations_edit')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_edit', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'radiologicalexaminations_delete')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'radiologicalexaminations_save')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'radiologicalexaminations_saveandsearchinpacs')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'radiologicalexaminations_goto')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'radiologicalexaminations_studydownload')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'radiologicalexaminations_create')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }

                    /////////////////////////////////////////////////////
                    //////// LOCAL RADIOLOGICAL EXAMINATIONS - CONTEXT MENU
                    /////////////////////////////////////////////////////
                    else if($el.data('item') === 'localradiologicalexaminations_show')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_show', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'localradiologicalexaminations_edit')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_edit', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'localradiologicalexaminations_delete')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'localradiologicalexaminations_save')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_show', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'localradiologicalexaminations_saveandclose')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_edit', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'localradiologicalexaminations_goto')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'localradiologicalexaminations_studydownload')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'localradiologicalexaminations_create')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }

                    /////////////////////////////////////////////////////
                    //////// MATERIALS - CONTEXT MENU
                    /////////////////////////////////////////////////////
                    else if($el.data('item') === 'materials_show')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_show', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'materials_edit')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_edit', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'materials_delete')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'materials_save')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_show', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'materials_saveandclose')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_edit', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'materials_goto')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'materials_studydownload')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'materials_create')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }

                    /////////////////////////////////////////////////////
                    //////// LOCAL MATERIALS - CONTEXT MENU
                    /////////////////////////////////////////////////////
                    else if($el.data('item') === 'localmaterials_show')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_show', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'localmaterials_edit')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_edit', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'localmaterials_delete')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'localmaterials_approve')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_show', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'localmaterials_reprobate')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_edit', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'localmaterials_goto')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'localmaterials_studydownload')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_show', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'localmaterials_create')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_edit', {id: row.id});
                        window.location.href = url; // simulate link click
                    }

                    /////////////////////////////////////////////////////
                    //////// PACS SERVER CONNECTIONS - CONTEXT MENU
                    /////////////////////////////////////////////////////
                    else if($el.data('item') === 'pacsserverconnections_show')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_show', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'pacsserverconnections_edit')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_edit', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'pacsserverconnections_delete')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'pacsserverconnections_approve')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_show', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'pacsserverconnections_reprobate')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_edit', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'pacsserverconnections_goto')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'pacsserverconnections_studydownload')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_show', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'pacsserverconnections_create')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_edit', {id: row.id});
                        window.location.href = url; // simulate link click
                    }

                    /////////////////////////////////////////////////////
                    //////// RADIOLOGICAL DIAGNOSTIC PATTERNS - CONTEXT MENU
                    /////////////////////////////////////////////////////
                    else if($el.data('item') === 'radiologicaldiagnosticpatterns_show')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_show', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'radiologicaldiagnosticpatterns_edit')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_edit', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'radiologicaldiagnosticpatterns_delete')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'radiologicaldiagnosticpatterns_approve')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_show', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'radiologicaldiagnosticpatterns_reprobate')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_edit', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'radiologicaldiagnosticpatterns_goto')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_delete', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'radiologicaldiagnosticpatterns_studydownload')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_show', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    else if($el.data('item') === 'radiologicaldiagnosticpatterns_create')
                    {
                        window.alert('item is: ' + '\n' + JSON.stringify($el) + '\n' + $el.data('item'));
                        var url = Routing.generate('simagd_lectura_edit', {id: row.id});
                        window.location.href = url; // simulate link click
                    }
                    // end
                },
            };  // --| build function object

        }(jQuery));

        // var destroy = function(el) {
        //     if (jQuery(el).attr('id') !== "undefined" && (jQuery(el).attr('id') === "pry2" || jQuery(el).attr('id') === "pry4")) {
        //         var $table = jQuery('#ryx_target_bstable');
        //         $table.bootstrapTable('destroy');
        //     }
        // }

        // function patientFormatResult(el) {
        //     var markup = '<div class="row-fluid">' +
        //                         // '<div class="span2"><img src="' + el.owner.avatar_url + '" /></div>' +
        //                         '<div class="span10">' +
        //                         '<div class="row-fluid">' +
        //                         '<div class="span6">' + el.full_name + '</div>' +
        //                         // '<div class="span3"><i class="fa fa-code-fork"></i> ' + el.forks_count + '</div>' +
        //                         // '<div class="span3"><i class="fa fa-star"></i> ' + el.stargazers_count + '</div>' +
        //                     '</div>';

        //     // if (el.description) {
        //     //     markup += '<div>' + el.description + '</div>';
        //     // }

        //     markup += '</div></div>';

        //     return markup;
        // }

        // function patientFormatSelection(el) {
        //     return el.full_name;
        // }

        // function displaySelection(data)
        // {
        //     var jt = [];
        //     if (!data.t || !parseInt(data.t, 10)) {
        //         jt.push('<div class="jumbotron">');
        //     }
        //     else {
        //         jt.push('<div class="jumbotron" style="background-color: #ebc5b9;">');
        //     }
        //     jt.push('<div class="container">', '<h2><strong><span class="text-primary-v4">', data.item.full_name, '</span></strong></h2>');
        //     if (!data.t || !parseInt(data.t, 10)) {
        //         jt.push('<strong><span class="badge badge-primary-v4" style="font-size: 25px;">', data.item.numero, '</span></strong>');
        //     }
        //     else {
        //         jt.push('<strong><span class="badge badge-emergency" style="font-size: 25px;">', data.item.numero, '</span></strong>');
        //     }
        //     jt.push('<p><a class="btn btn-primary-v4 " role="button">Crear solicitud</a></p>');
        //     jt.push('</div>', '</div>');
        //     return jt.join('');
        // }
    </script>

{% endautoescape %}
