{# empty Twig template #}

{% if admin.hasRoute('nota') and object.idEstadoDiagnostico.id in [6] and
		( ( ( app.user.getIdEmpleado.getIdTipoEmpleado.codigo == 'MED' or app.user.getIdEmpleado.getIdTipoEmpleado.codigo == 'TRY' ) and
		( is_granted('ROLE_MINSAL_SIMAGD_ADMIN_IMG_NOTA_DIAGNOSTICO_CREATE') or is_granted('ROLE_MINSAL_SIMAGD_ADMIN_IMG_NOTA_DIAGNOSTICO_EDIT') ) ) or
                    is_granted('ROLE_ADMIN') )
%}

    <a   href="{{ admin.generateObjectUrl('nota', object) }}" class="btn btn-sm btn-primary" title="Nota"> <i class="glyphicon glyphicon-comment"></i> Nota </a>

{% endif %}