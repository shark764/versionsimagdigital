{# empty Twig template #}

{% if object.requiereDiagnostico == false and admin.hasRoute('solicitarDiag') and
	( app.user.getIdEstablecimiento.getId == object.idAtenAreaModEstab.getIdEstablecimiento.getId ) and
        ( object.idUserReg.id == app.user.id or ( object.idUserMod is not null and object.idUserMod.id == app.user.id ) or is_granted('ROLE_ADMIN') ) and
		( ( is_granted('ROLE_MINSAL_SIMAGD_ADMIN_IMG_SOLICITUD_DIAGNOSTICO_CREATE') and is_granted('ROLE_MINSAL_SIMAGD_ADMIN_IMG_SOLICITUD_DIAGNOSTICO_EDIT') and
		is_granted('ROLE_MINSAL_SIMAGD_ADMIN_IMG_DIAGNOSTICO_VIEW') ) or is_granted('ROLE_ADMIN') )
%}

    <a   href="{{ admin.generateObjectUrl('solicitarDiag', object) }}" class="btn btn-sm btn-primary" title="Ver/Solicitar Diagnóstico"> <i class="glyphicon glyphicon-inbox"></i> Diagnóstico </a>

{% endif %}