{% autoescape false %}

	<script type="text/javascript">

        /////////////////////////////////////////////////////
        //////// SECOND MEDICAL OPINIONS - CONTEXT MENU
        /////////////////////////////////////////////////////
        (function ($) {
            /*
             *
             * @type Array
             * Global variables for plugin
             */
            window.__SECONDMEDICALOPINIONS_MENU_ACTIONEVENTS__ = {
                'click .secondmedicalopinions_show_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_segunda_opinion_medica_show', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .secondmedicalopinions_edit_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_segunda_opinion_medica_edit', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .secondmedicalopinions_delete_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_segunda_opinion_medica_delete', {id: row.id, confirmation: false});
                    // window.location.href = url; // simulate link click
                },
                'click .secondmedicalopinions_studydownload_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_lectura_show', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .secondmedicalopinions_create_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_segunda_opinion_medica_create', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .secondmedicalopinions_addtothis_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_segunda_opinion_medica_create', {id: row.id, preview: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
            };   // --| build data collection

        }(jQuery));
        
    </script>

{% endautoescape %}