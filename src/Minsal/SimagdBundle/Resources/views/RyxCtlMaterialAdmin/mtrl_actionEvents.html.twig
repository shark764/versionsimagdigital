{% autoescape false %}

	<script type="text/javascript">

        /////////////////////////////////////////////////////
        //////// MATERIALS - CONTEXT MENU
        /////////////////////////////////////////////////////
        (function ($) {
            /*
             *
             * @type Array
             * Global variables for plugin
             */
            window.__MATERIALS_MENU_ACTIONEVENTS__ = {
                'click .materials_show_action': function (e, value, row, index) {
                    window.console.log(e, value, row, index);
                    var url = Routing.generate('simagd_material_show', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .materials_edit_action': function (e, value, row, index) {
                    window.console.log(e, value, row, index);
                    var url = Routing.generate('simagd_material_edit', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .materials_delete_action': function (e, value, row, index) {
                    window.console.log(e, value, row, index);
                    var url = Routing.generate('simagd_material_delete', {id: row.id, mode: 'ajax', confirmation: false});
                    // window.location.href = url; // simulate link click
                },
                'click .materials_create_action': function (e, value, row, index) {
                    window.console.log(e, value, row, index);
                    var url = Routing.generate('simagd_material_create', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .materials_addtolocal_action': function (e, value, row, index) {
                    window.console.log(e, value, row, index);
                    var url = Routing.generate('simagd_material_create', {id: row.id, mode: 'ajax', confirmation: false});
                    // window.location.href = url; // simulate link click
                },
            };   // --| build data collection

        }(jQuery));
        
    </script>

{% endautoescape %}