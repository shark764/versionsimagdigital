{# empty Twig template #}

{% if admin.hasRoute('agregarEnMiCatalogo') and ( is_granted('ROLE_MINSAL_SIMAGD_ADMIN_IMG_CTL_MATERIAL_CREATE') or is_granted('ROLE_ADMIN') ) %}

    <a class="open-agregarEnLocalDialog text-success sonata-action-element" data-toggle="modal" href="#" data-id="{{ object.id }}" data-target="#agregar-en-catalogo-modal" title="Agregar proyección en Catálogo local">
        <i class="glyphicon glyphicon-plus-sign"></i>
        Agregar en local</a>

{% endif %}