{% autoescape false %}

    <script type="text/javascript">

        /////////////////////////////////////////////////////
        //////// LOCAL DIAGNOSTIC SERVICE EXAM SBY MODALITY - CONTEXT MENU
        /////////////////////////////////////////////////////
        (function ($) {
            /*
             *
             * @type Array
             * Global variables for plugin
             */
            window.__LOCALDIAGNOSTICSERVICEEXAMSBYMODALITY_MENU_ACTIONEVENTS__ = {
                'click .localdiagnosticserviceexamsbymodality_show_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_area_examen_establecimiento_show', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .localdiagnosticserviceexamsbymodality_edit_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_area_examen_establecimiento_edit', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .localdiagnosticserviceexamsbymodality_delete_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_area_examen_establecimiento_delete', {id: row.id, confirmation: false});
                    // window.location.href = url; // simulate link click
                },
                'click .localdiagnosticserviceexamsbymodality_create_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_area_examen_establecimiento_create', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
            };   // --| build data collection

        }(jQuery));
        
    </script>

{% endautoescape %}