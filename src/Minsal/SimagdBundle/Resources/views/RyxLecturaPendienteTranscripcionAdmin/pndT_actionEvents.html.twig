{% autoescape false %}

	<script type="text/javascript">

        /////////////////////////////////////////////////////
        //////// WRITE DIAGNOSIS WORKLIST - CONTEXT MENU
        /////////////////////////////////////////////////////
        (function ($) {
            /*
             *
             * @type Array
             * Global variables for plugin
             */
            window.__NONTRANSCRIBEDRESULTSWORKLIST_MENU_ACTIONEVENTS__ = {
                'click .nontranscribedresultsworklist_show_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_sin_transcribir_show', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .nontranscribedresultsworklist_edit_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_sin_transcribir_edit', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .nontranscribedresultsworklist_delete_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_sin_transcribir_delete', {id: row.id, mode: 'ajax', confirmation: false});
                    // window.location.href = url; // simulate link click
                },
                'click .nontranscribedresultsworklist_save_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_sin_transcribir_save', {id: row.id, mode: 'ajax', confirmation: false});
                    // window.location.href = url; // simulate link click
                },
                'click .nontranscribedresultsworklist_saveandclose_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_sin_transcribir_saveandclose', {id: row.id, mode: 'ajax', confirmation: false});
                    // window.location.href = url; // simulate link click
                },
                'click .nontranscribedresultsworklist_goto_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_diagnostico_edit', {id: row.id, worklist_id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .nontranscribedresultsworklist_studydownload_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_sin_transcribir_create', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
                'click .nontranscribedresultsworklist_create_action': function (e, value, row, index) {
                    window.console.log('e', e, '\n\nvalue', value, '\n\nrow', row, '\n\nindex', index);
                    var url = Routing.generate('simagd_sin_transcribir_create', {id: row.id, mode: 'standard'});
                    // window.location.href = url; // simulate link click
                },
            };   // --| build data collection

        }(jQuery));
        
    </script>

{% endautoescape %}